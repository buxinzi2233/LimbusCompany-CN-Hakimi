# 都市零协会汉化规范调研报告（三）：战斗技能类文本翻译与排版规约

## 1. 调研背景与机制本质
《边狱公司》（Limbus Company）的战斗系统高度数字化与词条化。技能描述文本不仅是供玩家阅读的文学说明，更是游戏底层数值逻辑与 UI 交互的直观投影。翻译中的任何一字之差（如“层数”与“强度”混淆、动词颠倒）都会直接引发玩家对战斗机制的严重误判。本报告区分上游格式、本工作区约定与实际资源映射；未核验的机制解释不作为引擎实现证明。

---

## 2. 核心规约：Buff/Debuff 三态动词铁律

在边狱公司的状态体系中，常见状态需要区分以下两个维度，具体条目不保证同时具有二者：
- **强度（Potency）**：决定状态触发时的威力、伤害量或暴击率提升数值。
- **层数（Count）**：决定状态可被触发/维持的回合次数或消耗次数。

为了实现零歧义表达，零协会确立了严格的动词分工体系：

| 作用对象 | 状态性质 | 强制规范动词 | 正确范例 | 严禁使用的错误动词 |
| :--- | :--- | :--- | :--- | :--- |
| **层数 (Count)** | 正面状态 (Buff) | **获得** | `自身获得2层[呼吸法] ` | 施加、赋予、得到、加上 |
| **层数 (Count)** | 负面状态 (Debuff) | **施加** | `对目标施加3层[流血] ` | 获得、给予、附着、烙印 |
| **强度 (Potency)** | 正面 / 负面状态 | **增加** | `使目标的震颤强度增加3点` | 获得、施加、叠加、提升 |
| **层数消耗/衰减** | 任意状态 | **消耗** / **降低** | `消耗5层[充能] ` | 按原文区分消耗与被动衰减 |

> **法则总结**：
> - 层数论“得失”：正面状态用“获得”，负面状态用“施加”，不按敌我区分。
> - 强度论“增减”：无论敌我正负，数值上升一律称“增加”，数值下降一律称“减少”。

---

## 3. 关键词空格后置与超链接触发机制

### 3.1 上游格式与本地约定

零协会[技能翻译规范](https://www.zeroasso.top/docs/translate/skills/)区分“名称后空格”与“方括号键值”；基准 `Passives.json` ID `999901` 含 `[Binding]`、`[Reduction]`。
本工作区按 AGENTS.md 使用中文方括号词条及后置空格；这是本地约定，不宣称上游禁止内部英文键。具体渲染和悬浮窗需实机验证，不从文档猜测解析器实现。

---

## 4. 技能条件状语与触发时机标准句式

以下映射按 `references/baseline-zh-CN/SkillTag.json` 的实际 ID 与 name 提取；不能将“攻击前”并入“使用时”，也不按英文含义猜测内部键。

| 条件标识符 | 基准中文 |
| --- | --- |
| `OnSucceedAttack` | `[命中时]` |
| `WhenUse` | `[使用时]` |
| `WinDuel` | `[拼点胜利]` |
| `BeforeAttack` | `[攻击前]` |
| `DefeatDuel` | `[拼点失败]` |
| `EnemyKill` | `[击杀敌方时]` |
| `CriticalOnSucceedAttack` | `[暴击命中时]` |
| `EndBattle` | `[回合结束时]` |
| `TurnStartBattle` | `[回合开始时]` |

---

## 5. 战斗数值与机制表达深度规约

### 5.1 混乱阈值（Stagger Threshold）
- **前移**：使敌人更容易进入混乱（`使目标的混乱阈值前移20`）。
- **后移**：延迟敌人混乱。
- **禁忌**：阈值是刻度点，严禁添加不规范量词（如“前移20%”必须明确是百分比还是绝对数值）。

### 5.2 威力（Power）体系区分
- **基础威力（Base Power）**：技能或硬币未投掷时的原始起点。
- **硬币威力（Coin Power）**：每掷出一枚正面硬币所累加的点数（如：`硬币威力+2`）。
- **拼点威力（Clash Power）**：仅在拼点对抗时生效的额外加成（如：`拼点威力+1`）。
- **最终威力（Final Power）**：拼点计算完成后直接施加在结算总点数上的数值。

### 5.3 物理与罪孽抗性（Resistances）表达
- **抗性数值增加**（如：斩击抗性 1.0 -> 1.5）：在游戏中表示受到的斩击伤害增加，中文必须准确表述为：`斩击抗性增加` 或 `受到更多斩击伤害`，严禁产生“抗击打能力提升/减伤”的反向误解。
- **抗性数值减少**（如：打击抗性 1.0 -> 0.5）：表示更耐揍，表述为：`打击抗性减少`。

---

## 6. 排版与长度压缩规范
1. **半角括号**：技能条件与补充说明统一使用半角括号 `( )`，紧贴文字，不留冗余空格，避免在手机端或狭窄战斗框体内被折行截断。
2. **连接符**：范围数值统一使用半角波浪号 `~`（如：`造成12~18点物理伤害`）。
