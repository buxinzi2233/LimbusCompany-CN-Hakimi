# 边狱公司（Limbus Company）本地化工作区规则 (LLC Standards)

本项目为《边狱公司》（Limbus Company）本地化汉化项目（都市零协会 LLC 架构）。所有进入本工作区进行文件查阅、代码编写、翻译修改或质量检验的 Agent 必须严格遵守以下工程规范与质量底线：

## 1. 绝对安全底线（Zero-Tolerance Rules）

1. **绝对封杀全角波浪号（L02 门禁，FATAL）**：
   - 严禁在任何 JSON 文件中出现全角波浪号 `～` (`\uFF5E`) 或 `〜` (`\u301C`)。
   - 必须统一使用半角波浪号 `~`。
   - *原因*：Unity TextMeshPro 字体引擎缺字，全角波浪号会导致字符方框 `□` 甚至触发渲染崩溃。

2. **占位符 1:1 严格对齐（L03 门禁，FATAL）**：
   - 所有动态占位符（如 `{0}`, `{1}`, `{Slot}`, `{Key}` 等）必须与韩文原版保持数量、标识符一致，且大括号必须闭合。

3. **Unity TextMeshPro 标签严格配对与 LIFO 嵌套（L05 门禁，FATAL）**：
   - 所有成对标签必须正确闭合：`<color=...></color>`, `<b></b>`, `<i></i>`, `<size=...></size>`, `<mark=...></mark>`。
   - 严禁交叉闭合（例如禁止 `<b><color></b></color>`）。
   - 单标签（自闭合）除外：`<sprite name="...">`, `<br>`。

## 2. 机制与词条翻译规范（Localization Invariants）

4. **关键词后置半角空格（L04 门禁）**：
   - 所有方括号包裹的战斗关键词 `[关键词] ` 必须紧跟一个半角空格。
   - 例：`使目标陷入[沉沦] 状态`（正确），`使目标陷入[沉沦]状态`（错误）。
   - *原因*：游戏引擎依靠词条后置空格截断词法单元以弹出词条悬浮窗说明。若无空格，浮窗无法触发。

5. **动词三元法则（L06 门禁）**：
   - **正面状态层数/次数**：一律用 **获得**（例：自身获得2层[呼吸法] ）。严禁使用“施加”、“赋予”。
   - **负面状态层数/次数**：一律用 **施加**（例：对目标施加3层[流血] ）。严禁使用“获得”。
   - **强度/数值变化**：一律用 **增加** / **减少**（例：使目标的[破裂] 强度增加4）。严禁说“获得强度”或“施加强度”！
   - **层数消耗/衰减**：一律用 **消耗** / **降低**。

6. **标点与排版规范（L07 门禁）**：
   - 省略号必须使用双字符标准中文六点省略号 `……`（`\u2026\u2026`），严禁使用英文 `...` 或日文圈圈 `。。。`。
   - 引号使用全角双引号 `“”` 与单引号 `‘’`。
   - 破折号使用双破折号 `——`，严禁使用单破折号、全角减号 `－` 或 `--`。

7. **韩文未译残留检测（L08 门禁）**：
   - 正式提交前必须确保待发布文件无韩文谚文残留（除白名单字段如 `model` 模型标识符外）。

8. **机制术语零协会资料优先原则（严禁残留英文标识符）**：
   - 技能、被动、状态机制与恐慌描述中（如“获得X层[XXX]”、“对目标施加X层[XXX]”），**严禁在中文说明中残留英文内部标识符**（如 `[Vulnerable]`、`[Protection]`、`[Reduction]`、`[ChargeNoir]`、`[Laceration]` 等）。
   - **第一优先级（优先检索零协会基准）**：必须优先查阅都市零协会官方基准库与词典（`references/baseline-zh-CN/BattleKeywords.json`、`Bufs.json`），使用权威官方译名（例：`Vulnerable` -> `易损`，`Protection` -> `守护`，`Reduction` -> `虚弱`，`Enhancement` -> `强壮`，`Laceration` -> `流血`，`Vibration` -> `震颤`）。
   - **第二优先级（合理自主翻译）**：当且仅当零协会官方资料尚未收录新词条时，结合文学典故、世界观设定与韩文原义进行**合理精当的自行翻译**，并在所有相关文件（Skills、Passives、Bufs、BattleKeywords）中保持 100% 术语一致。

## 3. 必须执行的验证流程

- 修改或新增 JSON 后，必须在终端运行静态质检：
  ```bash
  python3 tools/linter.py
  ```
- 包含自动修复（适用于修复波浪号、标点与关键词空格）：
  ```bash
  python3 tools/linter.py --target workspace/LLC_zh-CN --fix
  ```
- 在完成改动后，必须运行全量 116 项 E2E 验证套件：
  ```bash
  python3 tests/e2e/run_tests.py
  ```
- 只有在 Linter 0 错误且 E2E 100% 通过的前提下，方可进行打包发布与部署操作。
